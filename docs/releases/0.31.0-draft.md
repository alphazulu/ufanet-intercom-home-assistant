# Ufanet Intercom 0.31.0 — draft release notes

> **DRAFT / NOT FOR PUBLICATION YET.** This document prepares the future release text. It does not authorize merge, tag or GitHub Release. PR #15 hard live-validation gates remain authoritative.

## Highlights

### Actionable incoming-call notifications

The Home Assistant Companion notification workflow has been expanded from passive call alerts to guarded actions:

- immediate text notification for a confirmed Ufanet call;
- private Home Assistant image replacement when the cached last-call JPEG becomes ready;
- optional **Open door** action restricted to the selected Ufanet device and revalidated immediately before `button.press`;
- **View camera** navigation to the selected same-device live camera;
- stable per-intercom notification tags and timeout-based removal of stale door actions;
- manual blueprint runs deliberately contain no physical door action.

Android real-call delivery, physical door opening, live-camera navigation and timeout replacement have been live-tested. iOS action delivery is not yet live-tested.

### Physical keys and passage history

For intercoms advertising physical-key recording support, the validation branch adds:

- numeric **Physical keys** sensor and read-only inventory;
- **Last key passage** timestamp;
- passage EventEntity, Home Assistant event and visual device trigger;
- **КЛЮЧИ / KEYS** Lovelace tab;
- selected-key passage history loaded below the key list;
- privacy-safe `list_physical_keys` and `get_physical_key_passages` response services;
- validation-only **Add physical key** and **Rename** workflows;
- FCM `reason=key_add` completion handling with immediate inventory refresh.

Live-confirmed read-only behavior includes a non-empty key inventory, the real passage schema (`key:str`, `key_name:str`, `time_passage:int`), selected-key filtering with `filters.key=<external_id>`, and two real passage timestamps rendered end to end in Lovelace.

### Experimental key-number field

The official Android client uses physical-key `external_id` when requesting history for one selected key. The KEYS tab currently exposes that value as an **experimental `number` field** for visual comparison.

The project has **not yet confirmed** that `external_id` is the same number printed on the physical key. Until that is checked against a known key, documentation and UI must treat this mapping as Experimental rather than Confirmed. Internal provider `id` remains hidden.

### Physical-key enrollment and rename are still validation-only

The Android-observed enrollment flow uses:

```text
POST /api/v4/key/skud/<id>/auto_collect/enable/
```

and opens a 60-second enrollment window. The real new-key side effect and `reason=key_add` push have not yet been live-confirmed.

The Android-observed rename flow uses:

```text
POST /api/v4/key/edit/
```

The Home Assistant validation service resolves an opaque `key_ref` from fresh inventory and requires post-write read-back before reporting verified success. The provider rename endpoint still needs a controlled live rename before it can be marked Confirmed.

Physical-key deletion remains deliberately unimplemented and outside the 0.31.0 release scope.

## Reliability and safety fixes

- separated physical-key capability, inventory and passage-history health so a temporary history failure cannot incorrectly mark a supported intercom as unsupported;
- registered the main Lovelace card and packaged extensions through the race-safe Lovelace resource path to avoid intermittent **Configuration error** after dashboard reloads;
- added Android Companion compatibility fixes found during real notification testing;
- preserved privacy boundaries for provider key IDs, FCM credentials, raw push payloads and tokenized media URLs;
- added regression coverage for opaque refs, selected-key history, physical-key frontend safety and release packaging.

## Documentation / API evidence updates

The API reference now distinguishes:

- **Confirmed** — directly exercised successfully;
- **Observed** — seen in client code or responses but not fully live-validated;
- **Inferred** — derived behavior requiring validation;
- **Experimental** — intentionally exposed for controlled validation while semantics remain unproven;
- **Not supported** — tested request form that failed.

For state-changing endpoints, HTTP success alone is not treated as proof of the intended physical/account side effect.

## Still required before publication

The following items must be resolved or explicitly reviewed/waived in PR #15 before this draft can become release notes:

1. remaining real-call notification race/mismatch/metadata safety checks;
2. determine whether the experimental key `number` matches the marking on a known physical key, or rename/remove that field;
3. live-test **Add physical key** with a new unregistered key;
4. capture the real `reason=key_add` completion and verify immediate inventory refresh;
5. live-test `rename_physical_key` with post-write verification;
6. inspect enrollment/rename error behavior;
7. perform the final synchronized version/cache-bust bump and rerun all CI on the exact release candidate.

Do not merge, tag or publish from this draft without explicit approval after the live-validation gates are complete.
